name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build /home/runner/work/slot_car_demo/slot_car_demo/web --file Dockerfile --tag my-image-name:$(date +%s)
